/** portal — en. Flat map of fully-qualified key → string. */
const portal: Record<string, string> = {
  "portal.action.signOut": "Sign out",
  "portal.nav.areas": "Areas",
};

export default portal;
