/** portal — si. Flat map of fully-qualified key → string. */
const portal: Record<string, string> = {
  "portal.action.signOut": "පිටවෙන්න",
  "portal.nav.areas": "ප්‍රදේශ",
};

export default portal;
