; vitest 4 pulls vite 8, whose optional peer @types/node (^20.19 || >=22.12)
; conflicts with the @types/node ^18 this service pins to match its Node 18
; runtime. npm ci enforces peers and fails the Docker build over a
; devDependency-only mismatch that cannot affect production.
legacy-peer-deps=true
