fastapi==0.111.0
uvicorn[standard]==0.29.0
numpy==1.26.4
pandas==2.2.2
scipy==1.13.0
sqlalchemy==2.0.30
scikit-learn==1.4.2
xgboost==2.0.3
joblib==1.4.2
python-dotenv==1.0.1
pydantic==2.7.1
PyJWT[crypto]==2.13.0

# PostgreSQL driver (Supabase). Binary wheel so the image needs no build tools.
psycopg2-binary==2.9.10
# Used by routers/explain.py to call the OpenAI API server-side.
# Was already present transitively via FastAPI's test client; declared now
# that production code depends on it.
httpx>=0.27
